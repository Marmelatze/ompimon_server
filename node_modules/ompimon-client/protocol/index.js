/**
 * @module ompimon
 * @submodule ompimon-client
 */

var
    util = require("util"),
    _ = require("underscore"),
    eventEmitter = require("events").EventEmitter

;

/**
 * @class Protocol
 * @namespace Ompimon.Client.Protocol
 * @constructor
 */
function Protocol() {
    this.init();
}

util.inherits(Protocol, eventEmitter);

_.extend(Protocol.prototype, {
    /**
     * Parse data recevied from client
     * @param socket {Websocket}
     * @param data {Buffer}
     */
    parse: function(socket, data) {

    }
});