var Action = require("./action").Action,
    util = require("util"),
    auth = require("ompimon-auth").auth,
    response = require("../response.js")
;

exports.Action = Info;

function Info() {
    Action.call(this);
}

util.inherits(Info, Action);

Info.prototype.process = function(socket, data) {

};

