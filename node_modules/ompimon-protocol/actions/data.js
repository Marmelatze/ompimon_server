var
    _ = require("underscore"),
    action = require("./action"),
    util = require("util")
;


module.exports = Data;

function Data() {
    action.call(this);
}

util.inherits(Data, action);


_.extend(Data.prototype, {

});