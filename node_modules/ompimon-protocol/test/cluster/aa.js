var Storage = require("ompimon-storage"),
    Monitor = require("ompimon-monitor")
    ;

global.config = {
    monitor: false
};

module.exports = {
    start: function(test) {
        global.monitor = new Monitor('nodeunit', process.pid);
        Storage.clearAllData(function() {
            test.done();
        });
    }
};