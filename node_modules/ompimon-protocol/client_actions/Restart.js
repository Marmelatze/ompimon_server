/**
 * @module ompimon
 * @submodule ompimon-protocol
 */

var
    util = require("util"),
    _ = require("underscore"),

    auth = require('ompimon-auth').auth,

    Storage = require("ompimon-storage"),
    Redis = require("redis"),

    parser = require("../parser"),
    response = require("../response"),

    action = require("./Action"),
    Abort = require("./Abort")
;


module.exports = Restart;

/**
 * Restarts a application, when supported by the application
 *
 * @class Restart
 * @namespace Ompimon.Protocol.ClientAction
 * @extends Ompimon.Protocol.ClientAction.Abort
 * @constructor
 * @author Florian Pfitzer<pfitzer@w3p.cc>
 */
function Restart() {
    Abort.call(this);
    this.action = "restart";
}

util.inherits(Restart, Abort);

_.extend(Restart.prototype, {

});